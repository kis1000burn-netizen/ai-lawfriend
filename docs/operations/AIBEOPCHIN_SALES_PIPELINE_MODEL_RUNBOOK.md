# Sales Pipeline Model Runbook (Product Phase **34-A**)

**한 줄**: lead→qualified→demo→proposal→negotiation→closed-won handoff 파이프라인 stage 정의율을 집계해 `salesPipelineReady` 게이트를 판정한다.

## 검증

```bash
npm run verify:aibeopchin-sales-pipeline-deal-desk-phase34a
```

**버전** **`34-A.1`**
