# Issue / Burden / Evidence Judgment Mapping Runbook (Product Phase **40-C**)

**한 줄**: 청구·쟁점·입증책임·증거·항변·결과 시나리오 각 부문에 `JudgmentReference`를 붙이고 `issueBurdenEvidenceJudgmentMappingReady` 게이트를 판정한다.

## 검증

```bash
npm run verify:aibeopchin-legal-outcome-assessment-phase40c
```

**경계**: `NO_JUDGMENTLESS_LEGAL_ASSESSMENT` · `noJudgmentFallbackAllowed: false`

**버전** **`40-C.2`**
